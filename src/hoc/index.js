import SectionWrapper from "./SectionWrapper"; // Importing the SectionWrapper component from its file location.

/**
 * Re-exporting the `SectionWrapper` component.
 *
 * This file acts like a central hub, making it easy to import the `SectionWrapper`
 * from one place instead of remembering its exact file location every time.
 *
 * It's like having a shortcut on your desktop that takes you directly to
 * an important file!
 */
export { SectionWrapper };
